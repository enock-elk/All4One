{
  "name": "all4one-command-center",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "All4One Command Center — PDF manager, Trello watcher, and case tools",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "firebase:emulators": "npx -y firebase-tools@latest emulators:start --only functions",
    "firebase:deploy:functions": "npx -y firebase-tools@latest deploy --only functions"
  },
  "dependencies": {
    "lucide-react": "^1.16.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
